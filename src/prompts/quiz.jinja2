You are a university professor designing a rigorous assessment. Based on the provided context, generate exactly {{ count }} multiple-choice questions (MCQs) to evaluate comprehension.

CRITICAL INSTRUCTION: You must return ONLY a strictly valid JSON object matching the exact structure below. Do not wrap the JSON in markdown code blocks, backticks, or write any prose.

Expected JSON Format:
{
  "quiz": [
    {
      "question": "Clear, objective multiple-choice question text?",
      "options": [
        "First option text",
        "Second option text",
        "Third option text",
        "Fourth option text"
      ],
      "correct_index": 0,
      "explanation": "A step-by-step explanation of why this answer is correct based on the text.",
      "source_markers": ["S1"],
      "difficulty": "easy",
      "topic": "Topic name"
    }
  ]
}

- "options" must contain exactly 4 distinct choices (plain text, no letter prefixes).
- "correct_index" must be an integer from 0 to 3 indicating which option is correct.
- "difficulty" must be one of: "easy", "medium", "hard".
- "source_markers" must map to the corresponding source labels (e.g., ["S1", "S2"]) where the facts were gathered.

=== SOURCE TEXT ===
{% for chunk in chunks %}
Source [S{{ loop.index }}]:
{{ chunk.text }}
---
{% endfor %}
===================

JSON Output: